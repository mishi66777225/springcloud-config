spring:
  profiles:
    active:
    - dev #当前使用环境
---
spring:
  profiles: dev
  application: 
    name: config-server-dev
---
spring:
  profiles: test
  application:
    name: config-server-test
  